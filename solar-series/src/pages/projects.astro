---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Projects - SeamusRW's Tech Playground">
	<section class="projects-header">
		<div class="container">
			<h1>Projects</h1>
			<p class="subtitle">AI-powered tools and experiments</p>
		</div>
	</section>

	<section class="projects-showcase">
		<div class="container">
			<div class="projects-grid">
				<a href="/brewmuse" class="project-card">
					<div class="project-content">
						<div class="project-icon">☕</div>
						<h2>BrewMuse</h2>
						<p class="project-type">AI Barista Guide</p>
						<p class="project-description">
							Master the art of exceptional coffee with AI-powered guidance for espresso, pour-overs, and
							specialty brewing methods.
						</p>
						<div class="tech-tags">
							<span class="tag">AI</span>
							<span class="tag">Coffee</span>
							<span class="tag">Guidance</span>
						</div>
						<span class="cta">Explore →</span>
					</div>
				</a>

				<a href="/coffeepal" class="project-card">
					<div class="project-content">
						<div class="project-icon">🌍</div>
						<h2>CoffeePal</h2>
						<p class="project-type">Coffee Discovery</p>
						<p class="project-description">
							Discover exceptional coffee and connect with local roasters in your area through personalized AI
							recommendations.
						</p>
						<div class="tech-tags">
							<span class="tag">AI</span>
							<span class="tag">Discovery</span>
							<span class="tag">Community</span>
						</div>
						<span class="cta">Explore →</span>
					</div>
				</a>
			</div>
		</div>
	</section>

	<section class="tech-stack">
		<div class="container">
			<h2>Built With</h2>
			<div class="stack-items">
				<div class="stack-item">
					<div class="stack-icon">🤖</div>
					<h3>AI & LLMs</h3>
					<p>Powered by advanced language models</p>
				</div>
				<div class="stack-item">
					<div class="stack-icon">🚀</div>
					<h3>Atom</h3>
					<p>Modern AI agent framework</p>
				</div>
				<div class="stack-item">
					<div class="stack-icon">🎨</div>
					<h3>Astro</h3>
					<p>Fast, modern static site generation</p>
				</div>
				<div class="stack-item">
					<div class="stack-icon">🔧</div>
					<h3>Web APIs</h3>
					<p>Microphone & camera integration</p>
				</div>
			</div>
		</div>
	</section>
</Layout>

<style>
	.projects-header {
		background: linear-gradient(135deg, #f5f7ff 0%, #ffffff 100%);
		padding: 4rem 2rem;
		text-align: center;
		border-bottom: 1px solid #e0e0e0;
	}

	.projects-header h1 {
		font-size: 3.5rem;
		margin-bottom: 0.5rem;
		background: linear-gradient(135deg, #6366f1, #4f46e5);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}

	.subtitle {
		font-size: 1.3rem;
		color: #4b5563;
		font-style: italic;
	}

	.projects-showcase {
		padding: 4rem 2rem;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
	}

	.projects-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
		gap: 2rem;
		margin-bottom: 4rem;
	}

	.project-card {
		background: #ffffff;
		border: 1px solid #e0e0e0;
		border-radius: 12px;
		padding: 2.5rem;
		transition: all 0.3s ease;
		color: inherit;
		text-decoration: none;
		display: flex;
		flex-direction: column;
		overflow: hidden;
		position: relative;
	}

	.project-card::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 3px;
		background: linear-gradient(90deg, #6366f1, #4f46e5);
		opacity: 0;
		transition: opacity 0.3s ease;
	}

	.project-card:hover {
		transform: translateY(-8px);
		border-color: #6366f1;
		box-shadow: 0 12px 32px rgba(99, 102, 241, 0.15);
	}

	.project-card:hover::before {
		opacity: 1;
	}

	.project-icon {
		font-size: 4rem;
		margin-bottom: 1rem;
	}

	.project-card h2 {
		margin-bottom: 0.5rem;
		font-size: 2rem;
		color: #1a1a1a;
	}

	.project-type {
		color: #6366f1;
		font-weight: 600;
		font-size: 0.9rem;
		text-transform: uppercase;
		letter-spacing: 1px;
		margin-bottom: 1rem;
		margin-top: 0;
	}

	.project-description {
		color: #4b5563;
		line-height: 1.7;
		margin-bottom: 1.5rem;
		flex-grow: 1;
		font-size: 1.05rem;
	}

	.tech-tags {
		display: flex;
		flex-wrap: wrap;
		gap: 0.75rem;
		margin-bottom: 1.5rem;
	}

	.tag {
		background: #f5f7ff;
		color: #6366f1;
		padding: 0.4rem 0.8rem;
		border-radius: 6px;
		font-size: 0.85rem;
		border: 1px solid #e0e0e0;
		transition: all 0.3s ease;
	}

	.project-card:hover .tag {
		background: #e0e7ff;
		border-color: #6366f1;
	}

	.cta {
		color: #6366f1;
		font-weight: 600;
		display: inline-block;
		transition: transform 0.3s ease;
		align-self: flex-start;
	}

	.project-card:hover .cta {
		transform: translateX(4px);
	}

	.tech-stack {
		padding: 4rem 2rem;
		background: #fafafa;
		border-top: 1px solid #e0e0e0;
	}

	.tech-stack h2 {
		text-align: center;
		margin-bottom: 3rem;
	}

	.stack-items {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 2rem;
	}

	.stack-item {
		background: #ffffff;
		border: 1px solid #e0e0e0;
		border-radius: 12px;
		padding: 2rem;
		text-align: center;
		transition: all 0.3s ease;
	}

	.stack-item:hover {
		border-color: #6366f1;
		transform: translateY(-4px);
		box-shadow: 0 8px 20px rgba(99, 102, 241, 0.1);
	}

	.stack-icon {
		font-size: 2.5rem;
		margin-bottom: 1rem;
	}

	.stack-item h3 {
		margin-bottom: 0.5rem;
		font-size: 1.3rem;
		color: #1a1a1a;
	}

	.stack-item p {
		color: #4b5563;
		font-size: 0.95rem;
		margin: 0;
	}

	@media (max-width: 768px) {
		.projects-header {
			padding: 2rem 1rem;
		}

		.projects-header h1 {
			font-size: 2.5rem;
		}

		.subtitle {
			font-size: 1.1rem;
		}

		.projects-grid {
			grid-template-columns: 1fr;
		}

		.project-card {
			padding: 1.5rem;
		}

		.project-icon {
			font-size: 3rem;
		}

		.project-card h2 {
			font-size: 1.5rem;
		}

		.tech-stack {
			padding: 2rem 1rem;
		}

		.stack-items {
			grid-template-columns: 1fr;
		}
	}
</style>